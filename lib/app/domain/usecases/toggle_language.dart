import 'package:dartz/dartz.dart';

import '../../../core/error/failures.dart';
import '../../../core/usecases/usecase.dart';

class ToggleLanguage extends UseCase {
  @override
  Future<Either<Failure, dynamic>?> call(params) {
    throw UnimplementedError();
  }
}
